services:
  fujin:
    image: fujin:resp_pubsub-${BUILD_TAG}
    build:
      context: ..
      args:
        GO_BUILD_TAGS: fujin,grpc,resp_pubsub,observability
    container_name: fujin
    ports:
      - "4848:4848/udp"
      - "4949:4949"
    volumes:
      - ./assets:/assets
    command: /assets/config/config-resp_pubsub.yaml
    restart: always