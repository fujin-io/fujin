services:
  zeromq-pub:
    build:
      context: ..
      dockerfile: resources/Dockerfile.zeromq
    container_name: zeromq-pub
    environment:
      ZEROMQ_ENDPOINT: tcp://*:5555
    ports:
      - "5555:5555"
    restart: unless-stopped

