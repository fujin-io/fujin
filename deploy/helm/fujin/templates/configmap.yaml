apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "fujin.fullname" . }}
  labels:
    {{- include "fujin.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- toYaml .Values.config | nindent 4 }}
